pipeline {
  agent any

  stages {

    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Test') {
      steps {
        bat 'gradlew clean test'
      }
    }
  }

  post {
    always {
      allure([
        results: [[path: 'build/allure-results']]
      ])
    }
  }
}
